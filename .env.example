# Domain Configuration
# For local development, use localhost
# For production, replace with your actual domain name
DOMAIN=localhost

# Application Configuration
# Generate a secure encryption key using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# IMPORTANT: Keep this key secure and backed up - losing it means losing access to encrypted data
GOOGLE_ENCRYPTION_KEY=your-encryption-key-here

# Production Settings
FLASK_ENV=production