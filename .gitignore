# Local ccd config/state (when developing with CCD_CONFIG_DIR pointed at the repo)
.ccd-config/

# Operator-provided OAuth client overrides (contains a client secret)
client_config.json
# Local shell env holding CCD_GOOGLE_CLIENT_SECRET. The 'env/' rule below
# only covers a directory of that name, not this file.
/env

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Local scratch notes and tool state (never commit: desktop.md holds OAuth client values)
.serena/
desktop.md
